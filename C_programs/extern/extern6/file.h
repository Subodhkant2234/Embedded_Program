#ifndef __FILE_H
#define __FILE_H
static  void put(void);  // It is useless to have a static function declaration in a header file.
#endif                   // Another way of doing it as give the function definition of function  static inline put() in header file and 
			 // include it to source file. make put as static inline in a header file and define there.

